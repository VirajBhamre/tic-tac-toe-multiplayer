{
  "name": "nakama-multiplayer-tic-tac-toe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild nakama/modules/main.ts --bundle --platform=neutral --outfile=dist/index.js --format=esm && npm run build --prefix public",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nakama-runtime": "github:heroiclabs/nakama-common#master"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
